spring:
  datasource:
    url: jdbc:postgresql://127.0.0.1:5432/postgres  # 테스트용 DB 지정
    username: postgres
    password: 483949
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
logging:
  level:
    root: INFO
    org.springdoc: DEBUG
    com.academy.k_academy_manager_spring.controller: INFO
